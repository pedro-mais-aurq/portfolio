{
  "name": "neres-portfolio",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "check": "astro check",
    "build": "astro build",
    "ci": "npm run check && npm run build",
    "preview": "astro preview",
    "test:static": "node scripts/validate-build.mjs",
    "test": "npm run check && npm run build && npm run test:static",
    "astro": "astro"
  },
  "dependencies": {
    "@fontsource/archivo": "^5.2.8",
    "@fontsource/big-shoulders": "^5.2.6",
    "@fontsource/jetbrains-mono": "^5.2.8",
    "@fontsource/shippori-mincho": "^5.2.8",
    "astro": "^7.0.8",
    "gsap": "^3.15.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "typescript": "^6.0.3"
  }
}
