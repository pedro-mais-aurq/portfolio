# Astro Starter Kit: Minimal

```sh
npm create astro@latest -- --template minimal
```

> 🧑‍🚀 **Seasoned astronaut?** Delete this file. Have fun!

## 🚀 Project Structure

Inside of your Astro project, you'll see the following folders and files:

```text
/
├── public/
├── src/
│   └── pages/
│       └── index.astro
└── package.json
```

Astro looks for `.astro` or `.md` files in the `src/pages/` directory. Each page is exposed as a route based on its file name.

There's nothing special about `src/components/`, but that's where we like to put any Astro/React/Vue/Svelte/Preact components.

Any static assets, like images, can be placed in the `public/` directory.

## 🧞 Commands

All commands are run from the root of the project, from a terminal:

| Command                   | Action                                           |
| :------------------------ | :----------------------------------------------- |
| `npm install`             | Installs dependencies                            |
| `npm run dev`             | Starts local dev server at `localhost:4321`      |
| `npm run build`           | Build your production site to `./dist/`          |
| `npm run preview`         | Preview your build locally, before deploying     |
| `npm run astro ...`       | Run CLI commands like `astro add`, `astro check` |
| `npm run astro -- --help` | Get help using the Astro CLI                     |

## 👀 Want to learn more?

Feel free to check [our documentation](https://docs.astro.build) or jump into our [Discord server](https://astro.build/chat).

## Comandos

- `npm install`
- `npm run dev` — servidor local
- `npm run build` — build de produção (gera `dist/`)
- `npm run preview` — pré-visualizar o build

## Pendências conhecidas (placeholders claramente identificados no código)

- Capas dos 3 projetos (`src/assets/projects/*.webp`) são placeholders gerados — substituir pelas capas reais.
- Logo do macaco no Epílogo é um placeholder SVG simples — substituir pelo arquivo final da marca.
- Pequenas legendas em japonês no Hero e em Sobre foram transcritas de uma referência em baixa resolução — vale confirmar o texto exato com o cliente (marcado com comentários `<!-- placeholder -->` no código).
- Ilustrações de personagens usadas em Contato/Hero/Sobre/Quote são fan-art de personagens de terceiros (Dragon Ball, Jujutsu Kaisen, Chainsaw Man/Bleach, Cowboy Bebop, Goodnight Punpun) enviadas pelo cliente — avaliar direitos de uso antes de publicar comercialmente.
- Link do WhatsApp assume DDI 55 a partir do número exibido no rodapé — confirmar formato.
